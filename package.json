{
    "scripts": {
        "client:dev": "bun run --cwd apps/client dev",
        "server:dev": "bun run --cwd apps/server start:dev",
        "dev": "bun run dev:client & bun run dev:server"
    },
    "dependencies": {
        "@vueuse/core": "^13.1.0",
        "@vueuse/nuxt": "^13.1.0"
    }
}